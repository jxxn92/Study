import math
numbers = 5
limit = 3
power = 2
lst = []
cnt = 0
def divisor_cnt(number, limit, power):
    for i in range(1, int(math.sqrt(number)+1)):
        








for i in range(1, numbers+1):
    answer = 0
    for i in range*len(1, numbers+1):
        digit_num = divisor_cnt(numbers, limit, power)
        answer += digit_num
print(answer)